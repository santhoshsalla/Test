# Application under test
baseUrl=https://your-app-under-test.example

# Browser settings
browser=chrome
headless=false

# Timeouts (seconds)
explicitWaitSeconds=15
pageLoadTimeoutSeconds=30

# Test data (update to match your catalog)
productA.name=Product A
productB.name=Product B

# Optional hooks for advanced scenarios (implementations are app dependent)
# cart.api.endpoint.pattern=/api/cart
# cart.storage.expiry.key=cartExpiryAt
