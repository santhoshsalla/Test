# NOTE: Add/override values via system properties (e.g., -DbaseUrl=...)

baseUrl=https://your-app-under-test.example
plpPath=/products
pdpPathTemplate=/products/%s
cartPath=/cart

browser=chrome
headless=false
implicitWaitSeconds=0
explicitWaitSeconds=15
