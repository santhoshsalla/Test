# Do not remove existing properties; only append/update as needed.

baseUrl=https://your-app-under-test.example
browser=chrome
headless=false
implicitWaitSeconds=0
explicitWaitSeconds=15
pageLoadTimeoutSeconds=30

# Optional endpoints/paths used by resilience tests (update to match AUT)
addToCartEndpointPattern=*/cart/add*
removeFromCartEndpointPattern=*/cart/remove*
fetchCartEndpointPattern=*/cart*
